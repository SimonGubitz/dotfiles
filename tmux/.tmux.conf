bind r source ~/.tmux.conf\; display "~/.tmux.conf sourced!"

set -g prefix M-s
set -g escape-time 0


# Remap to Vim Motion Keys
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R
